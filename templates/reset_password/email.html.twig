{% trans_default_domain 'admin' %}

<p>{{ 'To reset your password, please visit the following link'|trans }}</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

<p>{{ 'This link will expire in {expire_info}.'|trans({expire_info: resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle')}) }}</p>
